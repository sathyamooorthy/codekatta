#include <stdio.h>

int main()
{
    int n,start,add,total = 0;
    scanf("%d%d%d",&n,&start,&add);
    for(int i=0;i<n;i++)
    {
        total=total + start;
        start = start + add;
         }
    printf("%d",total);
}
